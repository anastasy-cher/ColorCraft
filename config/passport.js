// Se encarga de crear las cookies con passport
const passport = require('passport')
const strategy = require('passport-local').Strategy

// Traemos la bd para hacer consultas
const pool = ('../config/conexion.js')

// Encriptar
const bcrypt = ('')


